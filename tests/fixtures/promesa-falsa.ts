/**
 * **La frase que `/apoyar` tuvo que corregir**, conservada como fixture para que
 * el detector de `tests/promesas-sobre-datos.test.ts` tenga un control negativo
 * con la frase histórica y no con una inventada.
 *
 * Este archivo **no se importa desde `src/`** y no llega a ninguna salida
 * pública: lo lee un test como texto.
 */
export const PROMESA_QUE_ERA_FALSA =
  'La agenda es gratis y va a seguir siendo gratis, y no se guarda quién entró.';
